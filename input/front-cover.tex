\documentclass[a5paper]{article} % Change 'a4paper' to 'a5paper' to test other sizes

% --- REQUIRED PACKAGES ---
\usepackage[pass]{geometry}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{chessboard}
% Use the 'T1' font encoding for better character support
\usepackage[T1]{fontenc}

% Load the 'helvet' package to set the default sans-serif font to Helvetica
\usepackage{helvet}

% --- NEW: Define the custom color ---
% We define the Chess.com green using its RGB values.
\definecolor{ChesscomGreen}{RGB}{78, 120, 55}

% --- DOCUMENT BEGINS ---
\begin{document}

\begin{titlepage}
    %\thisforkey{empty}

    \begin{tikzpicture}[remember picture, overlay]

        % --- BACKGROUND IMAGE ---
        \node at (current page.center) {
            % For best results with colored text, you will want a light background.
            \includegraphics[width=\paperwidth, height=\paperheight]{cover_image.jpg}
        };

        % --- FOREGROUND TEXT (Top Right, Green) ---
        \node[
            name=titleblock, % Added a name for this node
            draw=ChesscomGreen,                 % COLOR CHANGED: Border color set to the custom green
            color=ChesscomGreen,
            line width=4pt,                     % Make the border thicker
            inner sep=7pt,                     % Add padding inside the border
            text width=0.7\textwidth,
            align=center,
            anchor=south east,
            xshift=-0.6cm,
            yshift=0.6cm
        ] at (current page.south east) {

            {\fontsize{34pt}{72pt}\selectfont \bfseries
                \textsf{Krystof126}
            }

            \vspace{0.25cm}

            % --- SUBTITLE (with internal padding) ---
            % We wrap the colorbox in a group {...} to keep the padding change local.
            {%
                % Set the padding. Adjust '10pt' to increase or decrease the space.
                \setlength{\fboxsep}{9pt}%
                \colorbox{ChesscomGreen}{%
                    % The parbox now has its width adjusted to account for the new padding.
                    \parbox{\linewidth-2\fboxsep}{%
                        \flushright
                        \color{white}\Large \textsf{Ses plus belles parties d'échecs sur \textbf{Chess}.com}
                    }%
                }%
            }
        };

        % --- AUTHOR NAME (Green) ---
        \node[
            color=ChesscomGreen,
            align=center,
            rotate=90,              % Rotates the node content 90 degrees counter-clockwise
            anchor=south,           % Anchor the node at its new 'south' (bottom) edge
            xshift=0.1cm,             % Add some space to the left of the title block
        ] at (titleblock.west) {    % Position the node at the 'west' (left) side of the title block
             {\Large
                \textsf{Pascal GILLET}
             }
        };

        % --- DECORATIVE KNIGHT (Default Color) ---
        \node[
            color=ChesscomGreen,
            anchor=south west
        ] at (current page.south west) {

            %\hspace*{1cm}
            %\vspace*{1cm}

            \includegraphics[height=5cm]{pawn.png}
            %\resizebox{!}{5cm}{\BlackKnightOnWhite}
        };

    \end{tikzpicture}

\end{titlepage}

\end{document}